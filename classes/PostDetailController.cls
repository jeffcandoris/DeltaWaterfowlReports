public with sharing class PostDetailController {
  //@AuraEnabled
  //public static Post__c getPostDetails(String postId) {
  //  List<Post__c> postList = [SELECT Title__c, Featured_Image__c, Date_Published__c, Content_Rich__c FROM Post__c WHERE id = :postId];
  //  return postList.isEmpty() ? null : postList[0];
  //}
}