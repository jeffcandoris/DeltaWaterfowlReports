public with sharing class RaffleActive_Controller {
  //@AuraEnabled
  //public static List<Campaign> getRaffleList() {
  //  return [SELECT ID, Name, StartDate, EndDate, Description_Rich__c, Raffle_Ticket_Price__c, Featured_Image__c FROM Campaign WHERE isactive=true AND RecordType.Name='Raffle' AND EndDate >= TODAY];
  //}
}